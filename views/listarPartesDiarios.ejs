<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Partes Diarios - Regomax</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <!-- DataTables CSS -->
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.7/css/dataTables.bootstrap5.min.css">
    <link rel="stylesheet" href="https://cdn.datatables.net/buttons/2.4.2/css/buttons.bootstrap5.min.css">
    
    <!-- Custom styles -->
    <link rel="stylesheet" href="/assets/css/home.css">
    
    <style>
        /* Estilos para impresión */
        @media print {
            .no-print {
                display: none !important;
            }
            
            body {
                font-size: 12pt;
            }
            
            .badge {
                border: 1px solid #000;
                padding: 2px 5px;
            }
            
            .badge.bg-success {
                background-color: #fff !important;
                color: #000 !important;
                border-color: #198754;
            }
            
            .badge.bg-danger {
                background-color: #fff !important;
                color: #000 !important;
                border-color: #dc3545;
            }
            
            .badge.bg-warning {
                background-color: #fff !important;
                color: #000 !important;
                border-color: #ffc107;
            }
            
            .badge.bg-secondary {
                background-color: #fff !important;
                color: #000 !important;
                border-color: #6c757d;
            }
        }
        
        /* Checkbox para seleccionar partes */
        .parte-checkbox {
            width: 18px;
            height: 18px;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary no-print">
        <div class="container-fluid">
            <a class="navbar-brand d-flex align-items-center" href="/home">
                <img src="/assets/img/regomax.png" alt="Regomax Logo" class="bg-white rounded p-1">
                <span class="ms-2 fw-bold">Regomax S.A.</span>
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto align-items-center">
                    <li class="nav-item user-welcome me-3">
                        <span class="text-light">BIENVENIDO/A, <b><%= username %></b></span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#"><i class="bi bi-globe me-1"></i> Ver sitio</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#"><i class="bi bi-key me-1"></i> Cambiar contraseña</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" id="logout-link"><i class="bi bi-box-arrow-right me-1"></i> Cerrar sesión</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="container my-4">
        <div class="row mb-4">
            <div class="col">
                <div class="menu-container p-4" id="printableArea">
                    <div class="d-flex justify-content-between align-items-center mb-4 no-print">
                        <h4 class="menu-heading mb-0">
                            <i class="bi bi-clipboard me-2"></i><%= title %>
                        </h4>
                        <div class="d-flex gap-2">
                            <!-- Botones de exportación -->
                            <div class="dropdown">
                                <button class="btn btn-success dropdown-toggle" type="button" id="exportarBtn" data-bs-toggle="dropdown" aria-expanded="false">
                                    <i class="bi bi-file-earmark-arrow-down me-2"></i>Exportar
                                </button>
                                <ul class="dropdown-menu" aria-labelledby="exportarBtn">
                                    <li><a class="dropdown-item" href="#" id="btnExportarSeleccionados">Exportar seleccionados</a></li>
                                    <li><a class="dropdown-item" href="#" id="btnExportarTodos">Exportar todos</a></li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item" href="#" id="btnImprimirSeleccionados">Imprimir seleccionados</a></li>
                                </ul>
                            </div>
                            
                            <!-- Filtro por estado -->
                            <div class="dropdown">
                                <button class="btn btn-outline-primary dropdown-toggle" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-expanded="false">
                                    Filtrar por estado
                                </button>
                                <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                                    <li><a class="dropdown-item <%= typeof estadoActual === 'undefined' ? 'active' : '' %>" href="/partes-diarios">Todos</a></li>
                                    <li><a class="dropdown-item <%= estadoActual === 'pendiente' ? 'active' : '' %>" href="/partes-diarios/estado/pendiente">Pendientes</a></li>
                                    <li><a class="dropdown-item <%= estadoActual === 'aprobado' ? 'active' : '' %>" href="/partes-diarios/estado/aprobado">Aprobados</a></li>
                                </ul>
                            </div>
                            
                            <a href="/partes-diarios/nuevo" class="btn btn-primary">
                                <i class="bi bi-plus-circle me-2"></i>Nuevo Parte Diario
                            </a>
                        </div>
                    </div>
                    
                    <!-- Header para impresión -->
                    <div class="d-none d-print-block text-center mb-4">
                        <h2>REGOMAX S.A.</h2>
                        <h3>Listado de Partes Diarios</h3>
                        <p><strong>Fecha de impresión:</strong> <%= new Date().toLocaleDateString() %></p>
                    </div>
                    
                    <% if(partesPorFecha && partesPorFecha.length > 0) { %>
                        <div class="accordion no-print" id="accordionPartesDiarios">
                            <% partesPorFecha.forEach((fechaObj, index) => { %>
                                <% const fechaKey = fechaObj.fecha; %>
                                <div class="accordion-item">
                                    <h2 class="accordion-header" id="heading<%= fechaKey.replace(/-/g, '') %>">
                                        <button class="accordion-button <%= index === 0 ? '' : 'collapsed' %>" type="button" 
                                                data-bs-toggle="collapse" 
                                                data-bs-target="#collapse<%= fechaKey.replace(/-/g, '') %>" 
                                                aria-expanded="<%= index === 0 ? 'true' : 'false' %>" 
                                                aria-controls="collapse<%= fechaKey.replace(/-/g, '') %>">
                                            <div class="d-flex align-items-center justify-content-between w-100">
                                                <div>
                                                    <strong><i class="bi bi-calendar-date me-2"></i>Fecha: <%= fechaObj.fechaFormateada || fechaObj.fecha %></strong>
                                                    <span class="badge bg-primary rounded-pill ms-2"><%= fechaObj.partes.length %> partes</span>
                                                </div>
                                                <div class="form-check ms-3">
                                                    <input class="form-check-input fecha-checkbox" type="checkbox" value="<%= fechaKey %>" id="check-fecha-<%= fechaKey.replace(/-/g, '') %>">
                                                    <label class="form-check-label" for="check-fecha-<%= fechaKey.replace(/-/g, '') %>">
                                                        Seleccionar todos
                                                    </label>
                                                </div>
                                            </div>
                                        </button>
                                    </h2>
                                    <div id="collapse<%= fechaKey.replace(/-/g, '') %>" 
                                         class="accordion-collapse collapse <%= index === 0 ? 'show' : '' %>" 
                                         aria-labelledby="heading<%= fechaKey.replace(/-/g, '') %>" 
                                         data-bs-parent="#accordionPartesDiarios">
                                        <div class="accordion-body p-0">
                                            <div class="table-responsive">
                                                <table class="table table-striped table-hover mb-0 partes-table" data-fecha="<%= fechaKey %>">
                                                    <thead>
                                                        <tr>
                                                            <th class="no-print"><input type="checkbox" class="check-all-fecha" data-fecha="<%= fechaKey %>"></th>
                                                            <th>ID</th>
                                                            <th>Turno</th>
                                                            <th>Bolsones</th>
                                                            <th>Total Peso</th>
                                                            <th>Responsable</th>
                                                            <th>Estado</th>
                                                            <th class="no-print">Acciones</th>
                                                        </tr>
                                                    </thead>
                                                    <tbody>
                                                        <% fechaObj.partes.forEach(parte => { %>
                                                            <tr>
                                                                <td class="no-print">
                                                                    <input type="checkbox" class="parte-checkbox" value="<%= parte.id %>" data-fecha="<%= fechaKey %>" data-id="<%= parte.id %>">
                                                                </td>
                                                                <td><%= parte.id %></td>
                                                                <td><%= parte.turno %></td>
                                                                <td><%= parte.cantidadBolsones || 0 %></td>
                                                                <td><%= parte.totalPeso || 0 %> kg</td>
                                                                <td><%= parte.responsable %></td>
                                                                <td>
                                                                    <% if (parte.estado === 'pendiente') { %>
                                                                        <span class="badge bg-warning text-dark">Pendiente</span>
                                                                    <% } else if (parte.estado === 'aprobado') { %>
                                                                        <span class="badge bg-success">Aprobado</span>
                                                                    <% } else { %>
                                                                        <span class="badge bg-secondary">Desconocido</span>
                                                                    <% } %>
                                                                </td>
                                                                <td class="no-print">
                                                                    <div class="btn-group">
                                                                        <a href="/partes-diarios/detalle/<%= parte.id %>" class="btn btn-sm btn-info" title="Ver detalle">
                                                                            <i class="bi bi-eye"></i>
                                                                        </a>
                                                                        <a href="/partes-diarios/<%= parte.id %>" class="btn btn-sm btn-primary" title="Editar">
                                                                            <i class="bi bi-pencil"></i>
                                                                        </a>
                                                                        <button class="btn btn-sm btn-danger btn-delete" data-id="<%= parte.id %>" title="Eliminar">
                                                                            <i class="bi bi-trash"></i>
                                                                        </button>
                                                                        
                                                                        <% if (parte.estado === 'pendiente') { %>
                                                                            <button class="btn btn-sm btn-success btn-aprobar" data-id="<%= parte.id %>" title="Aprobar">
                                                                                <i class="bi bi-check-circle"></i>
                                                                            </button>
                                                                        <% } %>
                                                                    </div>
                                                                </td>
                                                            </tr>
                                                        <% }) %>
                                                    </tbody>
                                                </table>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            <% }) %>
                        </div>
                        
                        <!-- Tabla para impresión -->
                        <div class="d-none d-print-block">
                            <table class="table table-bordered">
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>Fecha</th>
                                        <th>Turno</th>
                                        <th>Bolsones</th>
                                        <th>Total Peso</th>
                                        <th>Responsable</th>
                                        <th>Estado</th>
                                    </tr>
                                </thead>
                                <tbody id="printTableBody">
                                    <!-- Se llena dinámicamente con JavaScript -->
                                </tbody>
                            </table>
                        </div>
                    <% } else { %>
                        <div class="alert alert-info">
                            <i class="bi bi-info-circle me-2"></i>No hay partes diarios registrados
                        </div>
                    <% } %>
                    
                    <% if (pagination && pagination.totalPages > 1) { %>
                        <nav aria-label="Navegación de partes diarios" class="mt-3 no-print">
                            <ul class="pagination justify-content-center">
                                <li class="page-item <%= pagination.page === 1 ? 'disabled' : '' %>">
                                    <a class="page-link" href="<%= typeof estadoActual !== 'undefined' ? `/partes-diarios/estado/${estadoActual}` : '/partes-diarios' %>?page=<%= pagination.page - 1 %>&limit=<%= pagination.limit %>" tabindex="-1">Anterior</a>
                                </li>
                                
                                <% for(let i = 1; i <= pagination.totalPages; i++) { %>
                                    <li class="page-item <%= pagination.page === i ? 'active' : '' %>">
                                        <a class="page-link" href="<%= typeof estadoActual !== 'undefined' ? `/partes-diarios/estado/${estadoActual}` : '/partes-diarios' %>?page=<%= i %>&limit=<%= pagination.limit %>"><%= i %></a>
                                    </li>
                                <% } %>
                                
                                <li class="page-item <%= pagination.page === pagination.totalPages ? 'disabled' : '' %>">
                                    <a class="page-link" href="<%= typeof estadoActual !== 'undefined' ? `/partes-diarios/estado/${estadoActual}` : '/partes-diarios' %>?page=<%= pagination.page + 1 %>&limit=<%= pagination.limit %>">Siguiente</a>
                                </li>
                            </ul>
                        </nav>
                    <% } %>
                    
                    <div class="d-flex justify-content-between align-items-center mt-3 no-print">
                        <div>
                            Mostrando <%= partesPorFecha ? partesPorFecha.reduce((total, fecha) => total + fecha.partes.length, 0) : 0 %> de <%= pagination ? pagination.total : 0 %> partes diarios
                        </div>
                        <div>
                            <form class="d-flex" action="<%= typeof estadoActual !== 'undefined' ? `/partes-diarios/estado/${estadoActual}` : '/partes-diarios' %>" method="GET">
                                <select class="form-select form-select-sm me-2" name="limit" onchange="this.form.submit()">
                                    <option value="10" <%= pagination && pagination.limit === 10 ? 'selected' : '' %>>10 por página</option>
                                    <option value="25" <%= pagination && pagination.limit === 25 ? 'selected' : '' %>>25 por página</option>
                                    <option value="50" <%= pagination && pagination.limit === 50 ? 'selected' : '' %>>50 por página</option>
                                    <option value="100" <%= pagination && pagination.limit === 100 ? 'selected' : '' %>>100 por página</option>
                                </select>
                                <input type="hidden" name="page" value="1">
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="row no-print">
            <div class="col">
                <a href="/home" class="btn btn-secondary">
                    <i class="bi bi-arrow-left me-2"></i>Volver al panel
                </a>
            </div>
        </div>
    </div>

    <!-- Modal de confirmación para eliminar -->
    <div class="modal fade" id="eliminarModal" tabindex="-1" aria-labelledby="eliminarModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header bg-danger text-white">
                    <h5 class="modal-title" id="eliminarModalLabel">Confirmar eliminación</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p>¿Está seguro que desea eliminar este parte diario?</p>
                    <p class="mb-0"><strong>Esta acción no se puede deshacer.</strong></p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-danger" id="confirmarEliminar">Eliminar</button>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Modal de confirmación para aprobar -->
    <div class="modal fade" id="aprobarModal" tabindex="-1" aria-labelledby="aprobarModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header bg-success text-white">
                    <h5 class="modal-title" id="aprobarModalLabel">Confirmar aprobación</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p>¿Está seguro que desea aprobar este parte diario?</p>
                    <p class="mb-0">Una vez aprobado, no se podrán realizar más cambios.</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-success" id="confirmarAprobar">Aprobar</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    
    <!-- html2pdf.js para exportación a PDF -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js"></script>
    
    <!-- Common JS (Para funcionalidades compartidas como logout) -->
    <script src="/assets/js/common.js"></script>
    
    <!-- Custom JS for this page -->
    <script src="/assets/js/listarPartesDiarios.js"></script>
</body>
</html>