<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ver Kit - Regomax</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/assets/css/home.css">
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand d-flex align-items-center" href="/home">
                <img src="/assets/img/regomax.png" alt="Regomax Logo" class="bg-white rounded p-1">
                <span class="ms-2 fw-bold">Regomax S.A.</span>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto align-items-center">
                    <li class="nav-item user-welcome me-3">
                        <span class="text-light">BIENVENIDO/A, <b><%= username %></b></span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#"><i class="bi bi-globe me-1"></i> Ver sitio</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" id="logout-link"><i class="bi bi-box-arrow-right me-1"></i> Cerrar sesión</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container-fluid my-4">
        <div class="row mb-4">
            <div class="col">
                <div class="menu-container p-4" id="kitDetails" data-kit-id="<%= kit.id %>">
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <h4 class="menu-heading mb-0">
                            <i class="bi bi-collection me-2"></i>Detalles del Kit
                        </h4>
                        <div>
                            <a href="/kits/editar/<%= kit.id %>" class="btn btn-warning">
                                <i class="bi bi-pencil me-2"></i>Editar
                            </a>
                            <button type="button" onclick="imprimirCodigo('<%= kit.barcodeBase64 %>', '<%= kit.codigo %>')" class="btn btn-primary ms-2">
                                <i class="bi bi-printer me-2"></i>Imprimir Código
                            </button>
                            <a href="/kits" class="btn btn-outline-secondary ms-2">
                                <i class="bi bi-arrow-left me-2"></i>Volver
                            </a>
                        </div>
                    </div>

                    <div id="alertPlaceholder"></div>

                    <!-- Información Básica -->
                    <div class="card mb-3 shadow-sm">
                        <div class="card-header" style="background-color: #17a2b8; color: white;">
                            <h5 class="mb-0"><i class="bi bi-info-circle me-2"></i>Información Básica</h5>
                        </div>
                        <div class="card-body bg-light">
                            <div class="row g-3">
                                <div class="col-md-3">
                                    <label class="text-muted small mb-1">Código</label>
                                    <div class="fw-bold fs-5 text-info"><%= kit.codigo %></div>
                                </div>
                                <div class="col-md-5">
                                    <label class="text-muted small mb-1">Nombre</label>
                                    <div class="fw-bold fs-5"><%= kit.nombre %></div>
                                </div>
                                <div class="col-md-2">
                                    <label class="text-muted small mb-1">Tipo</label>
                                    <div>
                                        <span class="badge <%= kit.tipo === 'Uso' ? 'bg-info' : 'bg-warning' %> fs-6">
                                            <%= kit.tipo %>
                                        </span>
                                    </div>
                                </div>
                                <div class="col-md-2">
                                    <label class="text-muted small mb-1">Stock</label>
                                    <div class="fs-4 <%= kit.cantidad_stock <= 0 ? 'text-danger' : 'text-info' %> fw-bold">
                                        <%= kit.cantidad_stock %>
                                        <% if (kit.cantidad_stock <= 0) { %>
                                        <i class="bi bi-exclamation-triangle-fill" title="Sin stock"></i>
                                        <% } %>
                                    </div>
                                </div>
                                <% if (kit.descripcion) { %>
                                <div class="col-12">
                                    <label class="text-muted small mb-1">Descripción</label>
                                    <div class="p-2 bg-white rounded border"><%= kit.descripcion %></div>
                                </div>
                                <% } %>
                            </div>
                        </div>
                    </div>

                    <!-- Componentes del Kit -->
                    <div class="card mb-3 shadow-sm">
                        <div class="card-header" style="background-color: #5bc0de; color: white;">
                            <h5 class="mb-0"><i class="bi bi-box-seam me-2"></i>Componentes del Kit</h5>
                        </div>
                        <div class="card-body bg-light">
                            <% if (kit.componentes && kit.componentes.length > 0) { %>
                            <div class="table-responsive">
                                <table class="table table-hover mb-0">
                                    <thead>
                                        <tr>
                                            <th>Código</th>
                                            <th>Nombre</th>
                                            <th>Tipo</th>
                                            <th class="text-center">Cantidad en Kit</th>
                                            <th class="text-end">Stock Disponible</th>
                                            <th class="text-end">Precio Unit.</th>
                                            <th class="text-end">Subtotal</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% kit.componentes.forEach(comp => { 
                                            const stockSuficiente = comp.cantidad_stock >= comp.cantidad;
                                            const subtotal = (comp.precio || 0) * comp.cantidad;
                                        %>
                                        <tr>
                                            <td><span class="badge bg-secondary"><%= comp.codigo %></span></td>
                                            <td><%= comp.nombre %></td>
                                            <td><span class="badge <%= comp.tipo === 'Uso' ? 'bg-info' : 'bg-warning' %>"><%= comp.tipo %></span></td>
                                            <td class="text-center">
                                                <span class="badge bg-primary"><%= comp.cantidad %></span>
                                            </td>
                                            <td class="text-end <%= !stockSuficiente ? 'text-danger fw-bold' : '' %>">
                                                <%= comp.cantidad_stock %>
                                                <% if (!stockSuficiente) { %>
                                                <i class="bi bi-exclamation-triangle-fill" title="Stock insuficiente"></i>
                                                <% } %>
                                            </td>
                                            <td class="text-end">$<%= parseFloat(comp.precio || 0).toLocaleString('es-AR', { minimumFractionDigits: 2 }) %></td>
                                            <td class="text-end fw-bold">$<%= subtotal.toLocaleString('es-AR', { minimumFractionDigits: 2 }) %></td>
                                        </tr>
                                        <% }) %>
                                    </tbody>
                                    <tfoot class="table-light">
                                        <tr>
                                            <td colspan="6" class="text-end fw-bold">Total Kit:</td>
                                            <td class="text-end fw-bold fs-5 text-primary">
                                                $<%= parseFloat(kit.precio || 0).toLocaleString('es-AR', { minimumFractionDigits: 2 }) %>
                                            </td>
                                        </tr>
                                    </tfoot>
                                </table>
                            </div>
                            <% } else { %>
                            <p class="text-muted text-center mb-0 py-3">
                                <i class="bi bi-inbox me-2"></i>No hay componentes asociados a este kit
                            </p>
                            <% } %>
                        </div>
                    </div>

                    <!-- Clasificación -->
                    <div class="card mb-3 shadow-sm">
                        <div class="card-header" style="background-color: #17a2b8; color: white;">
                            <h6 class="mb-0"><i class="bi bi-tags me-2"></i>Clasificación</h6>
                        </div>
                        <div class="card-body bg-light">
                            <div class="row">
                                <div class="col-md-6 mb-2">
                                    <label class="text-muted small">Categoría</label>
                                    <div><%= kit.categoria_nombre || 'Sin categoría' %></div>
                                </div>
                                <div class="col-md-6">
                                    <label class="text-muted small">Familia</label>
                                    <div><%= kit.familia_nombre || 'Sin familia' %></div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Ubicación -->
                    <div class="card mb-3 shadow-sm">
                        <div class="card-header" style="background-color: #5bc0de; color: white;">
                            <h6 class="mb-0"><i class="bi bi-geo-alt me-2"></i>Ubicación y Almacén</h6>
                        </div>
                        <div class="card-body bg-light">
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="text-muted small">Ubicación</label>
                                    <div><%= kit.ubicacion || 'No especificado' %></div>
                                </div>
                                <div class="col-md-6">
                                    <label class="text-muted small">Almacén por Defecto</label>
                                    <div><%= kit.almacen_nombre || 'No especificado' %></div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Metadata -->
                    <div class="card shadow-sm">
                        <div class="card-body" style="background-color: #e7f7f9;">
                            <small class="text-muted">
                                <i class="bi bi-person me-1"></i><strong>Creado por:</strong> <%= kit.responsable %><br>
                                <i class="bi bi-calendar me-1"></i><strong>Fecha:</strong> <%= new Date(kit.fecha_creacion).toLocaleString('es-AR', { dateStyle: 'long', timeStyle: 'short' }) %>
                            </small>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="/assets/js/common.js"></script>
    <script src="/assets/js/kitVer.js"></script>
</body>
</html>
