<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Regomax</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <!-- Custom styles -->
    <link rel="stylesheet" href="/assets/css/home.css">
    
    <!-- Estilos para impresión -->
    <style>
        @media print {
            .no-print {
                display: none !important;
            }
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary no-print">
        <div class="container-fluid">
            <a class="navbar-brand d-flex align-items-center" href="/home">
                <img src="/assets/img/regomax.png" alt="Regomax Logo" class="bg-white rounded p-1">
                <span class="ms-2 fw-bold">Regomax S.A.</span>
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto align-items-center">
                    <li class="nav-item user-welcome me-3">
                        <span class="text-light">BIENVENIDO/A, <b><%= username %></b></span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#"><i class="bi bi-globe me-1"></i> Ver sitio</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#"><i class="bi bi-key me-1"></i> Cambiar contraseña</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" id="logout-link"><i class="bi bi-box-arrow-right me-1"></i> Cerrar sesión</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="container my-4">
        <div class="row mb-4">
            <div class="col">
                <div class="menu-container p-4">
                    <div class="d-flex justify-content-between align-items-center mb-4 no-print">
                        <h4 class="menu-heading mb-0">
                            <i class="bi bi-receipt me-2"></i>Orden de Venta #OV-<%= orden.id %>
                        </h4>
                        <div>
                            <div class="btn-group">
                                <button type="button" class="btn btn-primary dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                                    <i class="bi bi-gear-fill me-1"></i> Acciones
                                </button>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    <li>
                                        <a class="dropdown-item" href="/ordenes/editar/<%= orden.id %>">
                                            <i class="bi bi-pencil me-2"></i>Editar
                                        </a>
                                    </li>
                                    <li>
                                        <button class="dropdown-item" id="btnImprimir">
                                            <i class="bi bi-printer me-2"></i>Imprimir
                                        </button>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" href="/despachos/orden/<%= orden.id %>">
                                            <i class="bi bi-truck me-2"></i>Ver despachos
                                        </a>
                                    </li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li>
                                        <button class="dropdown-item text-success" type="button" id="btnDespachoManual">
                                            <i class="bi bi-box-seam me-2"></i>Despacho Manual
                                        </button>
                                    </li>
                                </ul>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Contenido imprimible -->
                    <div id="printableArea">
                        <!-- Estado de la orden -->
                        <div class="mb-4">
                            <% let badgeClass = "bg-secondary"; %>
                            <% if (orden.estado === "abierta") { badgeClass = "bg-warning text-dark"; } %>
                            <% if (orden.estado === "en_logistica") { badgeClass = "bg-info"; } %>
                            <% if (orden.estado === "cerrada") { badgeClass = "bg-success"; } %>
                            <span class="badge <%= badgeClass %> fs-6">
                                Estado: <%= orden.estado ? orden.estado.toUpperCase().replace('_', ' ') : 'PENDIENTE' %>
                            </span>
                        </div>
                        
                        <!-- Detalles de la orden -->
                        <div class="row mb-4">
                            <div class="col-md-6">
                                <div class="card">
                                    <div class="card-header bg-primary text-white">
                                        <i class="bi bi-info-circle me-2"></i>Información General
                                    </div>
                                    <div class="card-body">
                                        <table class="table table-borderless">
                                            <tbody>
                                                <tr>
                                                    <th width="150">ID:</th>
                                                    <td>OV-<%= orden.id %></td>
                                                </tr>
                                                <% if (orden.codigo_venta) { %>
                                                <tr>
                                                    <th>Código de venta:</th>
                                                    <td><%= orden.codigo_venta %></td>
                                                </tr>
                                                <% } %>
                                                <tr>
                                                    <th>Fecha:</th>
                                                    <td><%= fechaUtils.formatearFechaLocal(orden.fecha) %></td>
                                                </tr>
                                                <tr>
                                                    <th>Cliente:</th>
                                                    <td><%= orden.cliente %></td>
                                                </tr>
                                                <% if (orden.cliente_final) { %>
                                                <tr>
                                                    <th>Cliente Final:</th>
                                                    <td><%= orden.cliente_final %></td>
                                                </tr>
                                                <% } %>
                                                <tr>
                                                    <th>Responsable:</th>
                                                    <td><%= orden.responsable %></td>
                                                </tr>
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-6 mt-3 mt-md-0">
                                <div class="card">
                                    <div class="card-header bg-primary text-white">
                                        <i class="bi bi-card-text me-2"></i>Observaciones
                                    </div>
                                    <div class="card-body">
                                        <% if (orden.observaciones) { %>
                                            <p class="mb-0"><%= orden.observaciones %></p>
                                        <% } else { %>
                                            <p class="text-muted mb-0">No hay observaciones registradas</p>
                                        <% } %>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Productos de la orden -->
                        <div class="card mb-4">
                            <div class="card-header bg-primary text-white">
                                <i class="bi bi-box-seam me-2"></i>Productos
                            </div>
                            <div class="card-body">
                                <div class="table-responsive">
                                    <table class="table table-striped">
                                        <thead>
                                            <tr>
                                                <th width="70">#</th>
                                                <th>Producto</th>
                                                <th width="150">Cantidad inicial</th>
                                                <th width="150">Cantidad restante</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% if (orden.productos && orden.productos.length > 0) { %>
                                                <% orden.productos.forEach((producto, index) => { %>
                                                    <tr>
                                                        <td><%= index + 1 %></td>
                                                        <td><%= producto.producto %></td>
                                                        <td><%= producto.cantidad_inicial %></td>
                                                        <td><%= producto.cantidad %></td>
                                                    </tr>
                                                <% }) %>
                                            <% } else { %>
                                                <tr>
                                                    <td colspan="4" class="text-center">No hay productos en esta orden</td>
                                                </tr>
                                            <% } %>
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Bolsones despachados -->
                        <div class="card mb-4">
                            <div class="card-header bg-info text-white">
                                <i class="bi bi-clipboard-check me-2"></i>Bolsones Despachados
                            </div>
                            <div class="card-body">
                                <div class="table-responsive">
                                    <table class="table table-striped">
                                        <thead>
                                            <tr>
                                                <th>#</th>
                                                <th>Código</th>
                                                <th>Producto</th>
                                                <th>Peso (kg)</th>
                                                <th>Fecha despacho</th>
                                                <th>Tipo</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% if (bolsonesDespachados && bolsonesDespachados.length > 0) { %>
                                                <% bolsonesDespachados.forEach((bolson, idx) => { %>
                                                    <tr>
                                                        <td><%= idx + 1 %></td>
                                                        <td><%= bolson.bolson_codigo %></td>
                                                        <td><%= bolson.producto %></td>
                                                        <td><%= parseFloat(bolson.peso).toFixed(2) %></td>
                                                        <td><%= fechaUtils.formatearFechaHoraLocal(bolson.fecha) %></td>
                                                        <td>
                                                            <% if (bolson.es_manual) { %>
                                                                <span class="badge bg-warning text-dark">Manual</span>
                                                            <% } else { %>
                                                                <span class="badge bg-info">Escaneado</span>
                                                            <% } %>
                                                        </td>
                                                    </tr>
                                                <% }) %>
                                            <% } else { %>
                                                <tr>
                                                    <td colspan="6" class="text-center">No hay bolsones despachados para esta orden</td>
                                                </tr>
                                            <% } %>
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="row no-print">
            <div class="col">
                <a href="/ordenes" class="btn btn-secondary">
                    <i class="bi bi-arrow-left me-2"></i>Volver a la lista
                </a>
            </div>
        </div>
    </div>

    <!-- Modal para confirmar eliminación -->
    <div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header bg-danger text-white">
                    <h5 class="modal-title" id="deleteModalLabel">Confirmar eliminación</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p>¿Está seguro que desea eliminar esta orden de venta? Esta acción no se puede deshacer.</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-danger" id="confirmDelete">Eliminar</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal para despacho manual -->
    <div class="modal fade" id="modalDespachoManual" tabindex="-1" aria-labelledby="modalDespachoManualLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header bg-success text-white">
                    <h5 class="modal-title" id="modalDespachoManualLabel">Despacho Manual (Sin Escaneo)</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <form id="formDespachoManual">
                        <div class="mb-3">
                            <label class="form-label">Orden #<%= orden.id %> - <%= orden.cliente %></label>
                        </div>
                        
                        <div class="mb-3">
                            <label for="observacionesDespacho" class="form-label">Observaciones:</label>
                            <textarea class="form-control" id="observacionesDespacho" rows="2" placeholder="Observaciones del despacho manual"></textarea>
                        </div>
                        
                        <div class="mb-3">
                            <h6>Productos disponibles:</h6>
                            <div class="table-responsive">
                                <table class="table table-bordered" id="tablaDespachoManual">
                                    <thead class="table-light">
                                        <tr>
                                            <th>Producto</th>
                                            <th>Cantidad Pendiente</th>
                                            <th>Cantidad a Despachar</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% if (orden.productos && orden.productos.length > 0) { %>
                                            <% orden.productos.forEach((prod, idx) => { 
                                                if (parseFloat(prod.cantidad) > 0) { %>
                                                <tr>
                                                    <td><%= prod.producto %></td>
                                                    <td><%= parseFloat(prod.cantidad).toFixed(2) %> kg</td>
                                                    <td>
                                                        <input type="number" class="form-control cantidad-despachar" 
                                                               data-producto="<%= prod.producto %>"
                                                               data-max="<%= parseFloat(prod.cantidad) %>"
                                                               min="0" max="<%= parseFloat(prod.cantidad) %>" 
                                                               step="0.01" value="0">
                                                    </td>
                                                </tr>
                                            <% } }) %>
                                        <% } else { %>
                                            <tr>
                                                <td colspan="3" class="text-center">No hay productos pendientes</td>
                                            </tr>
                                        <% } %>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-success" id="btnConfirmarDespachoManual">
                        <i class="bi bi-send me-1"></i>Procesar Despacho Manual
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    
    <!-- Common JS (Para funcionalidades compartidas como logout) -->
    <script src="/assets/js/common.js"></script>
    
    <!-- Custom JS for this page -->
    <script src="/assets/js/visualizarOrden.js"></script>
</body>
</html>