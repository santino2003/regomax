<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - Regomax</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <!-- Custom styles -->
    <link rel="stylesheet" href="/assets/css/home.css">
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand d-flex align-items-center" href="#">
                <img src="/assets/img/regomax.png" alt="Regomax Logo" class="bg-white rounded p-1">
                <span class="ms-2 fw-bold">Regomax S.A.</span>
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto align-items-center">
                    <li class="nav-item user-welcome me-3">
                        <span class="text-light">BIENVENIDO/A, <b><%= username %></b></span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#"><i class="bi bi-globe me-1"></i> Ver sitio</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" id="logout-link"><i class="bi bi-box-arrow-right me-1"></i> Cerrar sesión</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="container my-4">
        <div class="row g-4">
            <!-- Admin Menu Column -->
            <div class="col-lg-8">
                <div class="menu-container p-4">
                    <h4 class="menu-heading mb-4"><i class="bi bi-gear-fill me-2"></i>Administración</h4>
                    
                    <div class="accordion" id="adminAccordion">
                        <!-- Producción -->
                        <div class="accordion-item">
                            <h2 class="accordion-header">
                                <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseProduccion">
                                    <i class="bi bi-tools me-2"></i> Producción
                                </button>
                            </h2>
                            <div id="collapseProduccion" class="accordion-collapse collapse" data-bs-parent="#adminAccordion">
                                <div class="accordion-body">
                                    <% if (user && (user.role === 'admin' || user.permissions && user.permissions['bolsones:view'])) { %>
                                    <div class="submenu-item">
                                        <a href="/bolsones"><i class="bi bi-box-seam me-2"></i> Bolsones</a>
                                    </div>
                                    <% } %>
                                    
                                    <% if (user && (user.role === 'admin' || user.permissions && user.permissions['bolsones:create'])) { %>
                                    <div class="submenu-item">
                                        <a href="/bolsones/nuevo"><i class="bi bi-plus-circle me-2"></i> Crear bolson</a>
                                    </div>
                                    <% } %>
                                    
                                    <% if (user && (user.role === 'admin' || user.permissions && user.permissions['despachos:view'])) { %>
                                    <div class="submenu-item">
                                        <a href="/bolsones-despachados"><i class="bi bi-clipboard-check me-2"></i> Bolsones Despachados</a>
                                    </div>
                                    <% } %>
                                    
                                    <% if (user && (user.role === 'admin' || user.permissions && user.permissions['partes_diarios:create'])) { %>
                                    <div class="submenu-item">
                                        <a href="/partes-diarios/nuevo"><i class="bi bi-clipboard-data me-2"></i> Parte Diario</a>
                                    </div>
                                    <% } %>
                                    
                                    <% if (user && (user.role === 'admin' || user.permissions && user.permissions['partes_diarios:view'])) { %>
                                    <div class="submenu-item">
                                        <a href="/partes-diarios"><i class="bi bi-calendar-check me-2"></i> Ver Partes Diarios</a>
                                    </div>
                                    <% } %>
                                    
                                    <!-- Submenú de Reporte como título -->
                                    <% if (user && (user.role === 'admin' || user.permissions && (user.permissions['reportes:view'] || user.permissions['dias_habiles:view'] || user.permissions['reportes:view_ar']))) { %>
                                    <div class="submenu-item">
                                        <span class="submenu-title"><i class="bi bi-graph-up me-2"></i> Reporte</span>
                                    </div>
                                    
                                    <!-- Opciones de Reporte indentadas -->
                                    <% if (user && (user.role === 'admin' || user.permissions && user.permissions['reportes:view'] || user.permissions['reportes:view_ar'])) { %>
                                    <div class="submenu-item ms-4">
                                        <a href="/reporte-general"><i class="bi bi-clipboard-data me-2"></i> Reporte Productivo</a>
                                    </div>
                                    <% } %>
                                    
                                    <% if (user && (user.role === 'admin' || user.permissions && user.permissions['dias_habiles:view'])) { %>
                                    <div class="submenu-item ms-4">
                                        <a href="/dias-habiles"><i class="bi bi-calendar-check me-2"></i> Planificación</a>
                                    </div>
                                    <% } %>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Nuevo desplegable para Ingreso NFU -->
                        <div class="accordion-item">
                            <h2 class="accordion-header">
                                <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseNFU">
                                    <i class="bi bi-recycle me-2"></i> Ingreso de NFU
                                </button>
                            </h2>
                            <div id="collapseNFU" class="accordion-collapse collapse" data-bs-parent="#adminAccordion">
                                <div class="accordion-body">
                                    <% if (user && (user.role === 'admin' || user.permissions && user.permissions['reportes:view'])) { %>
                                    <div class="submenu-item">
                                        <a href="/nfu"><i class="bi bi-list-check me-2"></i> Lista NFU</a>
                                    </div>
                                    <% } %>
                                    <% if (user && (user.role === 'admin' || user.permissions && user.permissions['reportes:view'])) { %>
                                    <div class="submenu-item">
                                        <a href="/nfu/nuevo"><i class="bi bi-plus-circle me-2"></i> Registrar Ingreso NFU</a>
                                    </div>
                                    <% } %>
                                    <% if (user && (user.role === 'admin' || user.permissions && user.permissions['clientes_nfu:view'])) { %>
                                    <div class="submenu-item">
                                        <a href="/clientes-nfu"><i class="bi bi-people me-2"></i> Clientes NFU</a>
                                    </div>
                                    <% } %>
                                    <% if (user && (user.role === 'admin' || user.permissions && user.permissions['clientes_nfu:create'])) { %>
                                    <div class="submenu-item">
                                        <a href="/clientes-nfu/nuevo"><i class="bi bi-person-plus-fill me-2"></i> Nuevo Cliente NFU</a>
                                    </div>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Ventas y Logística -->
                        <div class="accordion-item">
                            <h2 class="accordion-header">
                                <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseVentas">
                                    <i class="bi bi-graph-up-arrow me-2"></i> Ventas y Logística
                                </button>
                            </h2>
                            <div id="collapseVentas" class="accordion-collapse collapse" data-bs-parent="#adminAccordion">
                                <div class="accordion-body">
                                    <% if (user && (user.role === 'admin' || user.permissions && user.permissions['ordenes:view'])) { %>
                                        <div class="submenu-item">
                                            <a href="/ordenes"><i class="bi bi-box-seam me-2"></i> Órdenes de Venta</a>
                                        </div>
                                    <% } %>
                                    <% if (user && (user.role === 'admin' || user.permissions && user.permissions['ordenes:create'])) { %>
                                    <div class="submenu-item">
                                        <a href="/ordenes/nueva"><i class="bi bi-file-earmark-plus me-2"></i> Cargar Órdenes de Venta</a>
                                    </div>
                                    <% } %>
                                    <% if (user && (user.role === 'admin' || user.permissions && user.permissions['despachos:create'])) { %>
                                        <div class="submenu-item">
                                            <a href="/despachos/nuevo"><i class="bi bi-truck me-2"></i> Despachar Bolsones</a>
                                        </div>
                                        <% } 
                                    %>
                                </div>
                            </div>
                        </div>
                         <!-- Compras -->
                        <div class="accordion-item">
                            <h2 class="accordion-header">
                                <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseCompras">
                                    <i class="bi bi-cart4 me-2"></i> Compras
                                </button>
                            </h2>
                            <div id="collapseCompras" class="accordion-collapse collapse" data-bs-parent="#adminAccordion">
                                <div class="accordion-body">
                                    <% if (user && (user.role === 'admin' || user.permissions && user.permissions['proveedor:view'])) { %>
                                    <div class="submenu-item">
                                        <a href="/proveedores"><i class="bi bi-person-lines-fill me-2"></i> Proveedores</a>
                                    </div>
                                    <% } %>
                                    <!-- Puedes agregar más opciones de compras aquí -->
                                </div>
                            </div>
                        </div>
                        
                        <!-- Almacén -->
                        <div class="accordion-item">
                            <h2 class="accordion-header">
                                <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseAlmacen">
                                    <i class="bi bi-box me-2"></i> Almacén
                                </button>
                            </h2>
                            <div id="collapseAlmacen" class="accordion-collapse collapse" data-bs-parent="#adminAccordion">
                                <div class="accordion-body">
                                    <% if (user && (user.role === 'admin' || user.permissions && user.permissions['familia:view'])) { %>
                                    <div class="submenu-item">
                                        <a href="/familias"><i class="bi bi-collection me-2"></i> Familias</a>
                                    </div>
                                    <% } %>
                                    <% if (user && (user.role === 'admin' || user.permissions && user.permissions['categoria:view'])) { %>
                                    <div class="submenu-item">
                                        <a href="/categorias"><i class="bi bi-tags me-2"></i> Categorías</a>
                                    </div>
                                    <% } %>
                                    <% if (user && (user.role === 'admin' || user.permissions && user.permissions['unidadMedida:view'])) { %>
                                    <div class="submenu-item">
                                        <a href="/unidades-medida"><i class="bi bi-rulers me-2"></i> Unidades de Medida</a>
                                    </div>
                                    <% } %>
                                    <% if (user && (user.role === 'admin' || user.permissions && user.permissions['almacen:view'])) { %>
                                    <div class="submenu-item">
                                        <a href="/almacenes"><i class="bi bi-building me-2"></i> Almacenes</a>
                                    </div>
                                    <% } %>
                                    <!-- Puedes agregar más opciones de almacén aquí -->
                                </div>
                            </div>
                        </div>
                        
                        <!-- Administración del Sistema - Solo visible para usuarios con permisos adecuados -->
                        <% if (user && (user.role === 'admin' || 
                                     (user.permissions && (user.permissions['users:view'] || 
                                                        user.permissions['configuracion:view'])))) { %>
                        <div class="accordion-item">
                            <h2 class="accordion-header">
                                <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseAdminSistema">
                                    <i class="bi bi-gear-fill me-2"></i> Administración del Sistema
                                </button>
                            </h2>
                            <div id="collapseAdminSistema" class="accordion-collapse collapse" data-bs-parent="#adminAccordion">
                                <div class="accordion-body">
                                    <% if (user && (user.role === 'admin' || (user.permissions && user.permissions['users:view']))) { %>
                                    <div class="submenu-item">
                                        <a href="/users"><i class="bi bi-people me-2"></i> Gestión de Usuarios</a>
                                    </div>
                                    <% } %>
                                    <% if (user) { %> <!-- El historial es visible para todos los usuarios autenticados -->
                                    <div class="submenu-item">
                                        <a href="/historial"><i class="bi bi-clock-history me-2"></i> Historial de Acciones</a>
                                    </div>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                        <% } %>

                        <!-- Ocultar secciones de menú vacías si el usuario no tiene permisos para ninguna opción -->
                        <script>
                            document.addEventListener('DOMContentLoaded', function() {
                                // Obtener todas las secciones de acordeón
                                const accordionSections = document.querySelectorAll('.accordion-item');
                                
                                // Para cada sección, verificar si tiene elementos de menú
                                accordionSections.forEach(section => {
                                    const menuItems = section.querySelectorAll('.submenu-item');
                                    if (menuItems.length === 0) {
                                        // Si no hay elementos de menú, ocultar la sección completa
                                        section.style.display = 'none';
                                    }
                                });
                            });
                        </script>
                    </div>
                </div>
            </div>
            
            <!-- Recent Actions Column -->
            <div class="col-lg-4">
                <div class="menu-container p-4 h-100">
                    <h4 class="menu-heading mb-4"><i class="bi bi-clock-history me-2"></i>Acciones recientes</h4>
                    
                    <div class="card action-card mb-3">
                        <div class="card-body">
                            <h5 class="card-title fw-bold text-primary">
                                <i class="bi bi-person-circle me-2"></i>Mis acciones
                            </h5>
                            
                            <% if (typeof accionesRecientes !== 'undefined' && accionesRecientes.length > 0) { %>
                                <ul class="list-group list-group-flush mt-3">
                                    <% accionesRecientes.forEach(function(accion) { %>
                                        <li class="list-group-item px-0">
                                            <div class="d-flex align-items-center">
                                                <% if (accion.accion === 'crear') { %>
                                                    <span class="badge bg-success me-2">Creación</span>
                                                <% } else if (accion.accion === 'editar') { %>
                                                    <span class="badge bg-warning text-dark me-2">Edición</span>
                                                <% } else if (accion.accion === 'eliminar') { %>
                                                    <span class="badge bg-danger me-2">Eliminación</span>
                                                <% } else if (accion.accion === 'login') { %>
                                                    <span class="badge bg-info text-dark me-2">Login</span>
                                                <% } else if (accion.accion === 'logout') { %>
                                                    <span class="badge bg-secondary me-2">Logout</span>
                                                <% } else { %>
                                                    <span class="badge bg-primary me-2"><%= accion.accion %></span>
                                                <% } %>
                                                <div>
                                                    <div><%= accion.descripcion %></div>
                                                    <small class="text-muted"><%= accion.fecha %></small>
                                                </div>
                                            </div>
                                        </li>
                                    <% }); %>
                                </ul>
                                <!-- Se eliminó el botón "Ver todo" -->
                            <% } else { %>
                                <p class="card-text text-muted">Ninguna acción disponible</p>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Common JS (Para funcionalidades compartidas como logout) -->
    <script src="/assets/js/common.js"></script>
    
    <!-- Original custom scripts -->
    <script src="/assets/js/home.js"></script>
</body>
</html>
