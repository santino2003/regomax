<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <!-- Custom styles -->
    <link rel="stylesheet" href="/assets/css/home.css">
    
    <!-- Estilos específicos para impresión -->
    <style>
        @media print {
            .no-print {
                display: none !important;
            }
            .print-only {
                display: block !important;
            }
            body {
                padding: 0;
                margin: 0;
            }
            .container {
                width: 100%;
                max-width: 100%;
            }
        }
        .print-only {
            display: none;
        }
        .print-header {
            text-align: center;
            margin-bottom: 20px;
        }
        .page-footer {
            text-align: center;
            margin-top: 30px;
            font-size: 0.8rem;
            color: #666;
        }
    </style>
</head>
<body>
    <!-- Barra de navegación (sólo visible en pantalla) -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary no-print">
        <div class="container-fluid">
            <a class="navbar-brand d-flex align-items-center" href="/home">
                <img src="/assets/img/regomax.png" alt="Regomax Logo" class="bg-white rounded p-1">
                <span class="ms-2 fw-bold">Regomax S.A.</span>
            </a>
            <div class="d-flex">
                <button class="btn btn-success me-2" id="btnPrint">
                    <i class="bi bi-printer me-1"></i> Imprimir
                </button>
                <button class="btn btn-primary" id="btnExportPDF">
                    <i class="bi bi-file-earmark-pdf me-1"></i> Exportar a PDF
                </button>
            </div>
        </div>
    </nav>

    <!-- Contenido para imprimir -->
    <div id="printableArea" class="container my-4">
        <!-- Encabezado para impresión -->
        <div class="row print-only d-print-block print-header">
            <div class="col-12 text-center">
                <img src="/assets/img/regomax.png" alt="Regomax Logo" style="height: 60px;" class="mb-2">
                <h4 class="mb-0">REGOMAX S.A.</h4>
                <p class="small mb-0">Orden de Venta</p>
            </div>
        </div>
        
        <!-- Información de la orden -->
        <div class="row mb-4">
            <div class="col">
                <div class="card">
                    <div class="card-header bg-primary text-white">
                        <h5 class="card-title mb-0">
                            <i class="bi bi-receipt me-2"></i>Orden de Venta #<%= orden.codigo_venta || `OV-${orden.id}` %>
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                <table class="table table-borderless">
                                    <tbody>
                                        <tr>
                                            <th width="150">ID:</th>
                                            <td>OV-<%= orden.id %></td>
                                        </tr>
                                        <% if (orden.codigo_venta) { %>
                                        <tr>
                                            <th>Código de venta:</th>
                                            <td><%= orden.codigo_venta %></td>
                                        </tr>
                                        <% } %>
                                        <tr>
                                            <th>Fecha:</th>
                                            <td><%= new Date(orden.fecha).toLocaleDateString() %></td>
                                        </tr>
                                        <tr>
                                            <th>Cliente:</th>
                                            <td><%= orden.cliente %></td>
                                        </tr>
                                        <% if (orden.cliente_final) { %>
                                        <tr>
                                            <th>Cliente Final:</th>
                                            <td><%= orden.cliente_final %></td>
                                        </tr>
                                        <% } %>
                                    </tbody>
                                </table>
                            </div>
                            <div class="col-md-6">
                                <table class="table table-borderless">
                                    <tbody>
                                        <tr>
                                            <th width="150">Responsable:</th>
                                            <td><%= orden.responsable %></td>
                                        </tr>
                                        <tr>
                                            <th>Estado:</th>
                                            <td>
                                                <% let badgeClass = "bg-secondary"; %>
                                                <% if (orden.estado === "abierta") { badgeClass = "bg-warning text-dark"; } %>
                                                <% if (orden.estado === "en_logistica") { badgeClass = "bg-info"; } %>
                                                <% if (orden.estado === "cerrada") { badgeClass = "bg-success"; } %>
                                                <span class="badge <%= badgeClass %>">
                                                    <%= orden.estado ? orden.estado.toUpperCase().replace('_', ' ') : 'PENDIENTE' %>
                                                </span>
                                            </td>
                                        </tr>
                                        <tr>
                                            <th>Fecha de impresión:</th>
                                            <td><%= fechaActual %></td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Observaciones -->
        <% if (orden.observaciones) { %>
        <div class="row mb-4">
            <div class="col">
                <div class="card">
                    <div class="card-header bg-primary text-white">
                        <h5 class="card-title mb-0">
                            <i class="bi bi-card-text me-2"></i>Observaciones
                        </h5>
                    </div>
                    <div class="card-body">
                        <p class="mb-0"><%= orden.observaciones %></p>
                    </div>
                </div>
            </div>
        </div>
        <% } %>
        
        <!-- Productos de la orden -->
        <div class="row mb-4">
            <div class="col">
                <div class="card">
                    <div class="card-header bg-primary text-white">
                        <h5 class="card-title mb-0">
                            <i class="bi bi-box-seam me-2"></i>Productos
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th width="70">#</th>
                                        <th>Producto</th>
                                        <th width="150">Cantidad inicial (kg)</th>
                                        <th width="150">Cantidad restante (kg)</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% if (orden.productos && orden.productos.length > 0) { %>
                                        <% orden.productos.forEach((producto, index) => { %>
                                            <tr>
                                                <td><%= index + 1 %></td>
                                                <td><%= producto.producto %></td>
                                                <td><%= parseFloat(producto.cantidad_inicial).toFixed(2) %></td>
                                                <td><%= parseFloat(producto.cantidad).toFixed(2) %></td>
                                            </tr>
                                        <% }) %>
                                    <% } else { %>
                                        <tr>
                                            <td colspan="4" class="text-center">No hay productos en esta orden</td>
                                        </tr>
                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Bolsones despachados -->
        <% if (bolsonesDespachados && bolsonesDespachados.length > 0) { %>
        <div class="row mb-4">
            <div class="col">
                <div class="card">
                    <div class="card-header bg-primary text-white">
                        <h5 class="card-title mb-0">
                            <i class="bi bi-clipboard-check me-2"></i>Bolsones Despachados
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Código</th>
                                        <th>Producto</th>
                                        <th>Peso (kg)</th>
                                        <th>Fecha despacho</th>
                                        <th>Tipo</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% bolsonesDespachados.forEach((bolson, idx) => { %>
                                        <tr>
                                            <td><%= idx + 1 %></td>
                                            <td><%= bolson.bolson_codigo %></td>
                                            <td><%= bolson.producto %></td>
                                            <td><%= parseFloat(bolson.peso).toFixed(2) %></td>
                                            <td><%= new Date(bolson.fecha).toLocaleString() %></td>
                                            <td>
                                                <% if (bolson.es_manual) { %>
                                                    <span class="badge bg-warning text-dark">Manual</span>
                                                <% } else { %>
                                                    <span class="badge bg-info">Escaneado</span>
                                                <% } %>
                                            </td>
                                        </tr>
                                    <% }) %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <% } %>
        
        <!-- Pie de página con información adicional -->
        <div class="page-footer">
            <p>Orden de Venta #<%= orden.codigo_venta || `OV-${orden.id}` %> - Generada el <%= fechaActual %></p>
            <p>REGOMAX S.A. - Sistema de Gestión</p>
        </div>
    </div>

    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    
    <!-- html2pdf library -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js"></script>
    
    <!-- Custom JS para la exportación a PDF -->
    <script>
        // Variables globales para el PDF
        const ordenId = '<%= orden.id %>';
        const ordenCodigo = '<%= orden.codigo_venta || `OV-${orden.id}` %>';
    </script>
    <script src="/assets/js/ordenExportar.js"></script>
</body>
</html>