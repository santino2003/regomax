<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Centros de Costo - Regomax</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/assets/css/home.css">
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand d-flex align-items-center" href="/home">
                <img src="/assets/img/regomax.png" alt="Regomax Logo" class="bg-white rounded p-1">
                <span class="ms-2 fw-bold">Regomax S.A.</span>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto align-items-center">
                    <li class="nav-item user-welcome me-3">
                        <span class="text-light">BIENVENIDO/A, <b><%= username %></b></span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#"><i class="bi bi-globe me-1"></i> Ver sitio</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" id="logout-link"><i class="bi bi-box-arrow-right me-1"></i> Cerrar sesión</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    
    <!-- Main Content -->
    <div class="container my-4">
        <div class="row mb-4">
            <div class="col">
                <div class="menu-container p-4">
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <h4 class="menu-heading mb-0">
                            <i class="bi bi-building me-2"></i>Gestión de Centros de Costo
                        </h4>
                        <div>
                            <a href="/centros-costo/nuevo-vista" class="btn btn-primary">
                                <i class="bi bi-plus-circle me-2"></i>Nuevo Centro de Costo
                            </a>
                        </div>
                    </div>
                    
                    <div class="table-responsive">
                        <table class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>Nombre</th>
                                    <th>Estado</th>
                                    <th>Responsable</th>
                                    <th>Fecha Creación</th>
                                    <th>Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% if(centrosCosto && centrosCosto.length > 0) { %>
                                    <% centrosCosto.forEach(centro => { %>
                                        <tr>
                                            <td><%= centro.nombre %></td>
                                            <td>
                                                <span class="badge <%= centro.activo ? 'bg-success' : 'bg-secondary' %>">
                                                    <%= centro.activo ? 'Activo' : 'Inactivo' %>
                                                </span>
                                            </td>
                                            <td><%= centro.responsable || '-' %></td>
                                            <td><%= centro.fecha_creacion ? new Date(centro.fecha_creacion).toLocaleDateString('es-AR') : '-' %></td>
                                            <td>
                                                <div class="btn-group" role="group">
                                                    <a href="/centros-costo/<%= centro.id %>/ver" class="btn btn-sm btn-outline-primary" title="Ver">
                                                        <i class="bi bi-eye"></i>
                                                    </a>
                                                    <a href="/centros-costo/<%= centro.id %>/editar" class="btn btn-sm btn-outline-secondary" title="Editar">
                                                        <i class="bi bi-pencil"></i>
                                                    </a>
                                                    <button class="btn btn-sm btn-outline-danger btn-delete" data-id="<%= centro.id %>" title="Eliminar">
                                                        <i class="bi bi-trash"></i>
                                                    </button>
                                                </div>
                                            </td>
                                        </tr>
                                    <% }) %>
                                <% } else { %>
                                    <tr>
                                        <td colspan="5" class="text-center">No hay centros de costo registrados</td>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>
                        
                        <!-- Paginación -->
                        <% if (pagination && pagination.totalPages > 1) { %>
                            <nav aria-label="Navegación de centros de costo">
                                <ul class="pagination justify-content-center">
                                    <li class="page-item <%= pagination.page === 1 ? 'disabled' : '' %>">
                                        <a class="page-link" href="?page=<%= pagination.page - 1 %>&limit=<%= pagination.limit %>">Anterior</a>
                                    </li>
                                    <% for(let i = 1; i <= pagination.totalPages; i++) { %>
                                        <li class="page-item <%= pagination.page === i ? 'active' : '' %>">
                                            <a class="page-link" href="?page=<%= i %>&limit=<%= pagination.limit %>"><%= i %></a>
                                        </li>
                                    <% } %>
                                    <li class="page-item <%= pagination.page === pagination.totalPages ? 'disabled' : '' %>">
                                        <a class="page-link" href="?page=<%= pagination.page + 1 %>&limit=<%= pagination.limit %>">Siguiente</a>
                                    </li>
                                </ul>
                            </nav>
                        <% } %>
                    </div>
                    
                    <div class="mt-3">
                        <a href="/home" class="btn btn-secondary">
                            <i class="bi bi-arrow-left me-2"></i>Volver al inicio
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal de Confirmación -->
    <div class="modal fade" id="confirmModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Confirmar acción</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <p id="confirmMessage"></p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-danger" id="confirmBtn">Confirmar</button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="/assets/js/common.js"></script>
    <script>
        $(document).ready(function() {
            let actionType = '';
            let targetId = '';

            // Eliminar centro de costo
            $('.btn-delete').click(function() {
                targetId = $(this).data('id');
                actionType = 'delete';
                $('#confirmMessage').text('¿Está seguro que desea eliminar este centro de costo?');
                $('#confirmModal').modal('show');
            });

            // Confirmar acción
            $('#confirmBtn').click(function() {
                if (actionType === 'delete') {
                    $.ajax({
                        url: `/api/centros-costo/${targetId}`,
                        method: 'DELETE',
                        success: function(response) {
                            showAlert('Centro de costo eliminado exitosamente', 'success');
                            setTimeout(() => location.reload(), 1500);
                        },
                        error: function(xhr) {
                            const error = xhr.responseJSON?.error || 'Error al eliminar el centro de costo';
                            showAlert(error, 'danger');
                        }
                    });
                }
                $('#confirmModal').modal('hide');
            });
        });
    </script>
</body>
</html>
