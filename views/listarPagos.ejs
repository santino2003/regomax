<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pagos - Regomax</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <!-- Select2 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/select2-bootstrap-5-theme@1.3.0/dist/select2-bootstrap-5-theme.min.css" rel="stylesheet" />
    <!-- Custom styles -->
    <link rel="stylesheet" href="/assets/css/home.css">
    <style>
        .badge-tipo-pago {
            font-size: 0.85rem;
            padding: 0.4rem 0.6rem;
        }
        .tipo-recepcion {
            background-color: #0d6efd;
        }
        .tipo-adelanto {
            background-color: #ffc107;
            color: #000;
        }
        .tipo-saldo {
            background-color: #198754;
        }
        .table-hover tbody tr:hover {
            background-color: #f8f9fa;
        }
        .monto-destacado {
            font-weight: 600;
            color: #198754;
        }
        .resumen-card {
            border-left: 4px solid #0d6efd;
        }
        .sticky-top {
            position: sticky;
            top: 0;
            z-index: 10;
        }
        #tablaCuotasEditable input {
            font-size: 0.9rem;
        }
        .fecha-atrasada {
            color: #dc3545;
            font-weight: 600;
        }
        .fecha-atrasada i {
            margin-left: 0.25rem;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand d-flex align-items-center" href="/home">
                <img src="/assets/img/regomax.png" alt="Regomax Logo" class="bg-white rounded p-1">
                <span class="ms-2 fw-bold">Regomax S.A.</span>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto align-items-center">
                    <li class="nav-item user-welcome me-3">
                        <span class="text-light">BIENVENIDO/A, <b><%= username %></b></span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#"><i class="bi bi-globe me-1"></i> Ver sitio</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" id="logout-link"><i class="bi bi-box-arrow-right me-1"></i> Cerrar sesión</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    
    <!-- Main Content -->
    <div class="container-fluid my-4">
        <div class="row mb-4">
            <div class="col">
                <div class="menu-container p-4">
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <h4 class="menu-heading mb-0">
                            <i class="bi bi-cash-coin me-2"></i>Gestión de Pagos
                        </h4>
                        <div>
                            <a href="/pagos/exportar<%= Object.keys(filtros).length > 0 ? '?' + new URLSearchParams(filtros).toString() : '' %>" 
                               class="btn btn-success" 
                               target="_blank">
                                <i class="bi bi-file-earmark-excel me-2"></i>Exportar
                            </a>
                        </div>
                    </div>

                    <!-- Resumen de totales -->
                    <% 
                    // Helper para formatear montos con miles separados por punto y decimales por coma
                    function formatoMoneda(monto) {
                        const num = Number(monto) || 0;
                        return new Intl.NumberFormat('es-AR', {
                            minimumFractionDigits: 2,
                            maximumFractionDigits: 2
                        }).format(num);
                    }

                    const totalGeneral = totales && totales.total_general ? totales.total_general : 0;
                    const totalRecepcion = totales && totales.total_recepcion ? totales.total_recepcion : 0;
                    const totalAdelanto = totales && totales.total_adelanto ? totales.total_adelanto : 0;
                    const totalSaldo = totales && totales.total_saldo ? totales.total_saldo : 0;

                    const resumenesPorMoneda = Array.isArray(totalesPorMoneda) && totalesPorMoneda.length > 0
                        ? totalesPorMoneda
                        : [{
                            moneda: (pagos[0] && pagos[0].moneda) ? pagos[0].moneda : 'ARS',
                            total_general: totalGeneral,
                            total_recepcion: totalRecepcion,
                            total_adelanto: totalAdelanto,
                            total_saldo: totalSaldo
                        }];
                    %>

                    <% resumenesPorMoneda.forEach(resumen => { %>
                        <div class="row mb-4">
                            <div class="col-md">
                                <div class="card resumen-card">
                                    <div class="card-body">
                                        <h6 class="card-subtitle mb-2 text-muted">Total General</h6>
                                        <h4 class="card-title mb-0 monto-destacado">
                                            <%= resumen.moneda || 'ARS' %>
                                            <%= formatoMoneda(resumen.total_general) %>
                                        </h4>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md">
                                <div class="card">
                                    <div class="card-body">
                                        <h6 class="card-subtitle mb-2 text-muted">Por Recepción</h6>
                                        <h5 class="card-title mb-0">
                                            <%= resumen.moneda || 'ARS' %>
                                            <%= formatoMoneda(resumen.total_recepcion) %>
                                        </h5>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md">
                                <div class="card">
                                    <div class="card-body">
                                        <h6 class="card-subtitle mb-2 text-muted">Adelantos</h6>
                                        <h5 class="card-title mb-0">
                                            <%= resumen.moneda || 'ARS' %>
                                            <%= formatoMoneda(resumen.total_adelanto) %>
                                        </h5>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md">
                                <div class="card">
                                    <div class="card-body">
                                        <h6 class="card-subtitle mb-2 text-muted">Saldos Completos</h6>
                                        <h5 class="card-title mb-0">
                                            <%= resumen.moneda || 'ARS' %>
                                            <%= formatoMoneda(resumen.total_saldo) %>
                                        </h5>
                                    </div>
                                </div>
                            </div>
                        </div>
                    <% }) %>

                    <!-- Filtros -->
                    <div class="card mb-4">
                        <div class="card-header bg-light">
                            <h5 class="card-title mb-0">
                                <i class="bi bi-funnel me-2"></i>Filtros
                            </h5>
                        </div>
                        <div class="card-body">
                            <form id="filtrosForm" method="GET" action="/pagos/listar">
                                <div class="row g-3">
                                    <div class="col-md-6 col-lg-2">
                                        <label for="pagado" class="form-label">Estado</label>
                                        <select class="form-select" id="pagado" name="pagado">
                                            <option value="" <%= !filtros.pagado || filtros.pagado === '' || filtros.pagado === 'false' ? 'selected' : '' %>>Pendientes</option>
                                            <option value="true" <%= filtros.pagado === 'true' ? 'selected' : '' %>>Pagados</option>
                                            <option value="all" <%= filtros.pagado === 'all' ? 'selected' : '' %>>Todos</option>
                                        </select>
                                    </div>
                                    <div class="col-md-6 col-lg-2">
                                        <label for="proveedor_id" class="form-label">Proveedor</label>
                                        <select class="form-select" id="proveedor_id" name="proveedor_id">
                                            <option value="">Todos</option>
                                            <% proveedores.forEach(proveedor => { %>
                                                <option value="<%= proveedor.id %>" 
                                                        <%= filtros.proveedor_id == proveedor.id ? 'selected' : '' %>>
                                                    <%= proveedor.nombre %>
                                                </option>
                                            <% }) %>
                                        </select>
                                    </div>
                                    <div class="col-md-6 col-lg-2">
                                        <label for="tipo_pago" class="form-label">Tipo de Pago</label>
                                        <select class="form-select" id="tipo_pago" name="tipo_pago">
                                            <option value="">Todos</option>
                                            <option value="RECEPCION" <%= filtros.tipo_pago === 'RECEPCION' ? 'selected' : '' %>>Recepción</option>
                                            <option value="ADELANTO" <%= filtros.tipo_pago === 'ADELANTO' ? 'selected' : '' %>>Adelanto</option>
                                            <option value="SALDO_COMPLETO" <%= filtros.tipo_pago === 'SALDO_COMPLETO' ? 'selected' : '' %>>Saldo Completo</option>
                                            <option value="MULTIPLE" <%= filtros.tipo_pago === 'MULTIPLE' ? 'selected' : '' %>>Pago Múltiple</option>
                                        </select>
                                    </div>
                                    <div class="col-md-6 col-lg-2">
                                        <label for="medio_pago" class="form-label">Medio de Pago</label>
                                        <select class="form-select" id="medio_pago" name="medio_pago">
                                            <option value="">Todos</option>
                                            <option value="Efectivo" <%= filtros.medio_pago === 'Efectivo' ? 'selected' : '' %>>Efectivo</option>
                                            <option value="Transferencia" <%= filtros.medio_pago === 'Transferencia' ? 'selected' : '' %>>Transferencia</option>
                                        </select>
                                    </div>
                                    <div class="col-md-6 col-lg-2">
                                        <label for="fecha_desde" class="form-label">Desde</label>
                                        <input type="date" 
                                               class="form-control" 
                                               id="fecha_desde" 
                                               name="fecha_desde" 
                                               value="<%= filtros.fecha_desde %>">
                                    </div>
                                    <div class="col-md-6 col-lg-2">
                                        <label for="fecha_hasta" class="form-label">Hasta</label>
                                        <input type="date" 
                                               class="form-control" 
                                               id="fecha_hasta" 
                                               name="fecha_hasta" 
                                               value="<%= filtros.fecha_hasta %>">
                                    </div>
                                </div>
                                <div class="row mt-3">
                                    <div class="col-12">
                                        <button type="submit" class="btn btn-primary">
                                            <i class="bi bi-search me-1"></i>Buscar
                                        </button>
                                        <a href="/pagos/listar" class="btn btn-outline-secondary ms-2">
                                            <i class="bi bi-x-circle me-1"></i>Limpiar
                                        </a>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>

                    <!-- Información de resultados -->
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <div>
                            <p class="mb-0 text-muted">
                                Mostrando <%= pagos.length %> de <%= paginacion.totalItems %> pagos
                            </p>
                        </div>
                        <div class="d-flex align-items-center gap-2">
                            <small class="text-muted">
                                Seleccionados: <span id="pagosSeleccionadosCount">0</span> | Total: 
                                <span id="pagosSeleccionadosTotal">ARS 0,00</span>
                            </small>
                            <button type="button" class="btn btn-primary btn-sm" id="btnPagarSeleccionados" disabled>
                                <i class="bi bi-cash-coin me-1"></i>Pagar seleccionados
                            </button>
                        </div>
                    </div>

                    <!-- Tabla de pagos -->
                    <div class="table-responsive">
                        <table class="table table-striped table-hover">
                            <thead class="table-dark">
                                <tr>
                                    <th>
                                        <input class="form-check-input" type="checkbox" id="selectAllPagos" title="Seleccionar todos">
                                    </th>
                                    <th>Orden</th>
                                    <th>Proveedor</th>
                                    <th>Tipo</th>
                                    <th>Medio</th>
                                    <th>Bien</th>
                                    <th>Monto</th>
                                    <th>Fecha Pago</th>
                                    <th>Observación</th>
                                    <th>Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% if (pagos.length === 0) { %>
                                    <tr>
                                        <td colspan="10" class="text-center py-4">
                                            <i class="bi bi-inbox fs-1 text-muted"></i>
                                            <p class="text-muted mt-2">No se encontraron pagos con los filtros aplicados</p>
                                        </td>
                                    </tr>
                                <% } else { %>
                                    <% pagos.forEach(pago => { %>
                                        <tr>
                                            <td>
                                                <input class="form-check-input pago-checkbox" 
                                                       type="checkbox"
                                                       data-pago-id="<%= pago.id %>"
                                                       data-orden-id="<%= pago.orden_compra_id %>"
                                                       data-orden-codigo="<%= pago.orden_codigo %>"
                                                       data-proveedor-id="<%= pago.proveedor_id %>"
                                                       data-proveedor-nombre="<%= pago.proveedor_nombre %>"
                                                       data-bien-nombre="<%= pago.bien_nombre || '' %>"
                                                       data-bien-codigo="<%= pago.bien_codigo || '' %>"
                                                       data-moneda="<%= pago.moneda || 'ARS' %>"
                                                       data-monto="<%= pago.monto_pago %>"
                                                       data-tipo-pago="<%= pago.tipo_pago %>"
                                                       <%= pago.pagado ? 'disabled' : '' %>>
                                            </td>
                                            <td>
                                                <% if (pago.orden_compra_id) { %>
                                                    <a href="/ordenes-compra/<%= pago.orden_compra_id %>" 
                                                       class="text-decoration-none">
                                                        <%= pago.orden_codigo %>
                                                    </a>
                                                <% } else { %>
                                                    <span class="text-muted">-</span>
                                                <% } %>
                                            </td>
                                            <td><%= pago.proveedor_nombre || '-' %></td>
                                            <td>
                                                <% if (pago.tipo_pago === 'RECEPCION') { %>
                                                    <span class="badge tipo-recepcion badge-tipo-pago">
                                                        <i class="bi bi-box-seam me-1"></i>Recepción
                                                    </span>
                                                <% } else if (pago.tipo_pago === 'ADELANTO') { %>
                                                    <span class="badge tipo-adelanto badge-tipo-pago">
                                                        <i class="bi bi-wallet2 me-1"></i>Adelanto
                                                    </span>
                                                <% } else if (pago.tipo_pago === 'SALDO_COMPLETO') { %>
                                                    <span class="badge tipo-saldo badge-tipo-pago">
                                                        <i class="bi bi-check-circle me-1"></i>Saldo Completo
                                                    </span>
                                                <% } else if (pago.tipo_pago === 'MULTIPLE') { %>
                                                    <span class="badge bg-secondary badge-tipo-pago">
                                                        <i class="bi bi-collection me-1"></i>Pago Múltiple
                                                    </span>
                                                <% } %>
                                            </td>
                                            <td>
                                                <% if (pago.medio_pago === 'Efectivo') { %>
                                                    <span class="badge bg-success">
                                                        <i class="bi bi-cash me-1"></i>Efectivo
                                                    </span>
                                                <% } else if (pago.medio_pago === 'Transferencia') { %>
                                                    <span class="badge bg-primary">
                                                        <i class="bi bi-bank me-1"></i>Transferencia
                                                    </span>
                                                <% } else { %>
                                                    <span class="text-muted">-</span>
                                                <% } %>
                                            </td>
                                            <td>
                                                <% if (pago.bien_nombre) { %>
                                                    <%= pago.bien_nombre %>
                                                    <% if (pago.bien_codigo) { %>
                                                        <small class="text-muted d-block">(<%= pago.bien_codigo %>)</small>
                                                    <% } %>
                                                <% } else { %>
                                                    <span class="text-muted">-</span>
                                                <% } %>
                                            </td>
                                            <td class="monto-destacado">
                                                <%= pago.moneda || 'ARS' %>
                                                <%= formatoMoneda(pago.monto_pago) %>
                                                <% if (pago.tipo_pago === 'SALDO_COMPLETO' && pago.monto_total) { %>
                                                    <small class="text-muted d-block">
                                                        Total: <%= pago.moneda || 'ARS' %> <%= formatoMoneda(pago.monto_total) %>
                                                    </small>
                                                    <% if (pago.monto_adelanto) { %>
                                                        <small class="text-muted d-block">
                                                            Adelanto: <%= pago.moneda || 'ARS' %> <%= formatoMoneda(pago.monto_adelanto) %>
                                                        </small>
                                                    <% } %>
                                                <% } %>
                                            </td>
                                            <td>
                                                <% 
                                                // Comparar fecha de pago con la fecha actual
                                                const fechaPago = new Date(pago.fecha_pago);
                                                const fechaActual = new Date();
                                                // Normalizar las fechas a medianoche para comparar solo días
                                                fechaPago.setHours(0, 0, 0, 0);
                                                fechaActual.setHours(0, 0, 0, 0);
                                                const estaAtrasada = !pago.pagado && fechaPago < fechaActual;
                                                %>
                                                <span class="<%= estaAtrasada ? 'fecha-atrasada' : '' %>">
                                                    <%= new Date(pago.fecha_pago).toLocaleDateString('es-AR') %>
                                                    <% if (estaAtrasada) { %>
                                                        <i class="bi bi-exclamation-triangle-fill" title="Pago atrasado"></i>
                                                    <% } %>
                                                </span>
                                            </td>
                                            <td>
                                                <% if (pago.observaciones) { %>
                                                    <span><%= pago.observaciones %></span>
                                                <% } else { %>
                                                    <span class="text-muted">Sin observaciones</span>
                                                <% } %>
                                            </td>
                                            <td>
                                                <div class="btn-group" role="group">
                                                    <a href="/pagos/<%= pago.id %>" 
                                                       class="btn btn-sm btn-info" 
                                                       title="Ver detalles">
                                                        <i class="bi bi-eye"></i>
                                                    </a>
                                                    <% if (!pago.pagado) { %>
                                                        <button class="btn btn-sm btn-danger" 
                                                                onclick="marcarPagado(<%= pago.id %>)"
                                                                title="Marcar como pagado">
                                                            <i class="bi bi-check-circle"></i>
                                                        </button>
                                                        <button class="btn btn-sm btn-warning" 
                                                                onclick="abrirModalRefinanciar(<%= pago.id %>, '<%= pago.proveedor_nombre %>', <%= pago.monto_pago %>)"
                                                                title="Refinanciar pago">
                                                            <i class="bi bi-calendar-event"></i>
                                                        </button>
                                                    <% } %>
                                                </div>
                                            </td>
                                        </tr>
                                    <% }); %>
                                <% } %>
                            </tbody>
                        </table>
                    </div>

                    <!-- Paginación -->
                    <% if (paginacion.totalPages > 1) { %>
                        <nav aria-label="Paginación de pagos">
                            <ul class="pagination justify-content-center">
                                <li class="page-item <%= !paginacion.hasPrev ? 'disabled' : '' %>">
                                    <a class="page-link" 
                                       href="?page=<%= paginacion.currentPage - 1 %><%= Object.keys(filtros).map(k => filtros[k] ? '&' + k + '=' + encodeURIComponent(filtros[k]) : '').join('') %>">
                                        Anterior
                                    </a>
                                </li>
                                
                                <% 
                                const maxPagesToShow = 5;
                                let startPage = Math.max(1, paginacion.currentPage - Math.floor(maxPagesToShow / 2));
                                let endPage = Math.min(paginacion.totalPages, startPage + maxPagesToShow - 1);
                                
                                if (endPage - startPage < maxPagesToShow - 1) {
                                    startPage = Math.max(1, endPage - maxPagesToShow + 1);
                                }
                                
                                for (let i = startPage; i <= endPage; i++) { 
                                %>
                                    <li class="page-item <%= i === paginacion.currentPage ? 'active' : '' %>">
                                        <a class="page-link" 
                                           href="?page=<%= i %><%= Object.keys(filtros).map(k => filtros[k] ? '&' + k + '=' + encodeURIComponent(filtros[k]) : '').join('') %>">
                                            <%= i %>
                                        </a>
                                    </li>
                                <% } %>
                                
                                <li class="page-item <%= !paginacion.hasNext ? 'disabled' : '' %>">
                                    <a class="page-link" 
                                       href="?page=<%= paginacion.currentPage + 1 %><%= Object.keys(filtros).map(k => filtros[k] ? '&' + k + '=' + encodeURIComponent(filtros[k]) : '').join('') %>">
                                        Siguiente
                                    </a>
                                </li>
                            </ul>
                        </nav>
                    <% } %>

                </div>
            </div>
        </div>
    </div>

    <!-- Modal para marcar como pagado -->
    <div class="modal fade" id="modalMarcarPagado" tabindex="-1" aria-labelledby="modalMarcarPagadoLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="modalMarcarPagadoLabel">
                        <i class="bi bi-check-circle me-2"></i>Marcar Pago como Pagado
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3">
                        <label for="detallePago" class="form-label">Detalle del Pago (Opcional)</label>
                        <textarea class="form-control" 
                                  id="detallePago" 
                                  rows="3" 
                                  placeholder="Ingrese información adicional sobre el pago (ej: número de transferencia, banco, etc.)"></textarea>
                        <div class="form-text">
                            Este campo es opcional. Puede agregar información relevante sobre cómo se realizó el pago.
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-danger" onclick="confirmarMarcarPagado()">
                        <i class="bi bi-check-circle me-2"></i>Marcar como Pagado
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal para pago múltiple (comprobante único) -->
    <div class="modal fade" id="modalPagoMultiple" tabindex="-1" aria-labelledby="modalPagoMultipleLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header bg-primary text-white">
                    <h5 class="modal-title" id="modalPagoMultipleLabel">
                        <i class="bi bi-collection me-2"></i>Pago múltiple - Comprobante único
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="alert alert-info py-2">
                        <i class="bi bi-info-circle me-2"></i>
                        <small>Se registrará un único pago y se vinculará a los ítems seleccionados.</small>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Resumen de ítems seleccionados</label>
                        <div class="table-responsive" style="max-height: 240px; overflow-y: auto;">
                            <table class="table table-sm table-bordered mb-0">
                                <thead class="table-light sticky-top">
                                    <tr>
                                        <th>Orden</th>
                                        <th>Item</th>
                                        <th>Monto</th>
                                    </tr>
                                </thead>
                                <tbody id="listaPagosSeleccionados"></tbody>
                                <tfoot class="table-secondary">
                                    <tr>
                                        <td colspan="2" class="text-end"><strong>Total:</strong></td>
                                        <td><strong id="totalPagoMultiple">ARS 0,00</strong></td>
                                    </tr>
                                </tfoot>
                            </table>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label for="detallePagoMultiple" class="form-label">Detalle del Comprobante (Opcional)</label>
                        <textarea class="form-control" 
                                  id="detallePagoMultiple" 
                                  rows="3" 
                                  placeholder="Ej: Transferencia bancaria, número de comprobante, cuenta, etc."></textarea>
                        <div class="form-text">
                            Se agregará al detalle del pago junto con el listado de OC e ítems.
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-primary" onclick="confirmarPagoMultiple()">
                        <i class="bi bi-check-circle me-2"></i>Registrar pago único
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal para refinanciar pago -->
    <div class="modal fade" id="modalRefinanciar" tabindex="-1" aria-labelledby="modalRefinanciarLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header bg-warning">
                    <h5 class="modal-title" id="modalRefinanciarLabel">
                        <i class="bi bi-calendar-event me-2"></i>Refinanciar Pago
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="alert alert-info py-2">
                        <i class="bi bi-info-circle me-2"></i>
                        <small>La refinanciación creará nuevos pagos en cuotas y eliminará el pago original. Puede personalizar el monto y fecha de cada cuota.</small>
                    </div>
                    
                    <div class="row mb-2">
                        <div class="col-6">
                            <small class="text-muted">Proveedor:</small>
                            <div id="refinanciarProveedor" class="fw-bold"></div>
                        </div>
                        <div class="col-6">
                            <small class="text-muted">Monto Original:</small>
                            <div id="refinanciarMontoOriginal" class="text-success fw-bold fs-5"></div>
                        </div>
                    </div>

                    <hr class="my-2">

                    <div class="row mb-2">
                        <div class="col-12">
                            <label for="cantidadCuotas" class="form-label mb-1">Cantidad de Cuotas *</label>
                            <input type="number" 
                                   class="form-control form-control-sm" 
                                   id="cantidadCuotas" 
                                   min="1" 
                                   max="60"
                                   value="3"
                                   onchange="generarCuotas()"
                                   required>
                            <small class="form-text text-muted">Entre 1 y 60 cuotas</small>
                        </div>
                    </div>

                    <h6 class="mb-2 mt-2">
                        <i class="bi bi-list-ol me-2"></i>Detalle de Cuotas
                        <span class="badge bg-primary" id="badgeTotalCuotas">0</span>
                    </h6>

                    <div class="table-responsive" style="max-height: 300px; overflow-y: auto;">
                        <table class="table table-bordered table-sm mb-0">
                            <thead class="table-light sticky-top">
                                <tr>
                                    <th style="width: 100px;">#</th>
                                    <th style="width: 200px;">Fecha *</th>
                                    <th>Monto *</th>
                                </tr>
                            </thead>
                            <tbody id="tablaCuotasEditable">
                                <!-- Las cuotas se generarán dinámicamente aquí -->
                            </tbody>
                            <tfoot class="table-secondary">
                                <tr>
                                    <td class="text-end"><strong>Total:</strong></td>
                                    <td><strong id="totalCuotas">$0.00</strong></td>
                                    <td></td>
                                </tr>
                            </tfoot>
                        </table>
                    </div>

                    <div class="row mt-2">
                        <div class="col-6">
                            <small class="text-muted">Total Refinanciado:</small>
                            <div id="totalRefinanciado" class="text-primary fw-bold">$0.00</div>
                        </div>
                        <div class="col-6">
                            <small class="text-muted">Diferencia:</small>
                            <div id="diferenciaRefinanciado" class="fw-bold">$0.00</div>
                        </div>
                    </div>

                    <div class="mt-2">
                        <label for="observacionesRefinanciar" class="form-label mb-1">Observaciones Generales</label>
                        <textarea class="form-control form-control-sm" 
                                  id="observacionesRefinanciar" 
                                  rows="2" 
                                  placeholder="Motivo de la refinanciación, acuerdos, etc."></textarea>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-warning" onclick="confirmarRefinanciar()">
                        <i class="bi bi-calendar-event me-2"></i>Refinanciar Pago
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal de Mensajes -->
    <div class="modal fade" id="modalMensaje" tabindex="-1" aria-labelledby="modalMensajeLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header" id="modalMensajeHeader">
                    <h5 class="modal-title" id="modalMensajeLabel">
                        <i class="bi bi-info-circle me-2" id="modalMensajeIcon"></i>
                        <span id="modalMensajeTitulo">Información</span>
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body" id="modalMensajeCuerpo">
                    <!-- El mensaje se insertará aquí -->
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal de Confirmación -->
    <div class="modal fade" id="modalConfirmacion" tabindex="-1" aria-labelledby="modalConfirmacionLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header bg-warning">
                    <h5 class="modal-title" id="modalConfirmacionLabel">
                        <i class="bi bi-exclamation-triangle me-2"></i>Confirmación
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body" id="modalConfirmacionCuerpo">
                    <!-- El mensaje de confirmación se insertará aquí -->
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-warning" id="btnConfirmarAccion">Confirmar</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <!-- jQuery (requerido para Select2) -->
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <!-- Select2 JS -->
    <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>
    
    <!-- Script de Pagos -->
    <script src="/assets/js/pagos.js"></script>
</body>
</html>
